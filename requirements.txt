# Python 3.9–3.12 (tested on 3.11). These ranges bound each dependency below a
# tested minimum and above the next major so an incompatible release can't be
# pulled in. The exact versions this project was last verified against are noted
# in each comment; `pip install -r requirements.txt` will resolve within range.

# Core ML stack
torch>=2.2,<3            # tested: 2.13
torchvision>=0.17,<1     # tested: 0.28 (must match the installed torch build)

# Data / numerics
numpy>=1.24,<3           # tested: 2.3.5
Pillow>=9.5,<13          # tested: 12.3

# Metrics, splitting, evaluation
scikit-learn>=1.3,<2     # tested: 1.9

# Plotting (confusion matrix, training curves)
matplotlib>=3.7,<4       # tested: 3.11

# --- Detection stage (only needed to (re)build the dataset's bounding boxes) ---
# MegaDetector is the default detector for scripts/fill_boxes_yolo.py. Its weights
# are a YOLOv5 checkpoint, so the yolov5 package is required; yolov5 still imports
# pkg_resources, which setuptools 81+ removed.
#   pip install "yolov5>=7.0,<8" "setuptools<81"
# Alternative COCO detector (--detector yolov8, src/detect.py):
#   pip install "ultralytics>=8.0,<9"
# OpenCV image preprocessing:
#   pip install "opencv-python-headless>=4.7,<5"
#
# Training, evaluation and prediction do NOT need any of the above: the committed
# dataset already carries its boxes in the manifest.
